# THROW → TLS MIGRATION ROADMAP

- **Status:** APPROVED — Stages 1-11 complete (see individual stage statuses for full evidence; Stage 9 closed as an explicit split verdict, not an unqualified pass, on the whole-turn CB-duplication finding tracked as issue #42). **Slice D1 (issue #37 guard) landed.** **Stage 12 (full validation matrix) complete** — every row V1-V27 addressed with fresh evidence on the final build; the TLS/ObjectHelper/ORS authority chain passes cleanly everywhere it's tested. Four rows carry pre-existing, already-decided exceptions (none new, none regressions): V7/issue #40 (entire-stack "all" phrasing unreachable), V17 whole-turn/issue #42 (CB duplicate promotion), V21 live-labeling/issue #41 (diagnostics-only mislabel), and V27/O7 (compound-turn CB-promotion risk — probabilistic, not reproduced this run). V6/V9/V10 remain source-review-validated per established precedent. No new TAKE or DROP regression attributable to this migration was observed (issue #38, a pre-existing unrelated TAKE bug, still exists independently). See Stage 12 status for the full per-row breakdown. **Stage 13 has not started.** No stage may be marked complete merely because it is planned — each still requires its own validation gate to actually be run and observed.
- **Derived from:** `THROW_TLS_MIGRATION_IMPLEMENTATION_PLAN.md` (the detailed planning contract; this roadmap is the staged sequence view of that contract and must not diverge from it).
- **Source state at planning:** `main` @ `41dff601d5c56516eee8a9e6a5281b7530b43724` (v1.92.8). Implementation is proceeding on branch `throw-migration-branch`.
- **Approval:** APPROVED by the user via direct conversational authorization on 2026-07-21, given after the GPT forensic review revision (Revision 1) was pushed. The user explicitly stated this authorization is to be treated as "ceremonial authority" in place of a file edit; this Revision 2 now syncs the file text to match. Each remaining stage still requires the plan's Gate A (source re-verification + explicit implementation instruction) before its slice is coded.
- **Precedent:** the completed DROP → TLS migration (12-step staged sequence, quarantine-first / inert-first / one-authority-layer-at-a-time). The supplied precedent file is the v1.91.84-checkpoint revision of that roadmap; its endpoint is corroborated by current source, where DROP is fully live through TLS.

## FINAL CONTRACT

AP recognizes THROW but has no object-operation authority. TLS owns THROW resolution and execution policy. ObjectHelper performs the authoritative mutation. ORS remains authoritative state. THROW moves a player-held ORS object — one unit by default, an explicitly requested quantity, or the entire stack — to the authoritative current Ground (active localspace → active site tile → current L0 grid cell). Over-stack, vague, malformed, contradictory, zero, negative, fractional, or otherwise unsupported quantity requests fail closed without mutation. Single-action turns only; compound queues do not enter the THROW TLS lane. Existing interpretation and narration of trailing target phrasing ("throw X at Y") are preserved unchanged: there is no authoritative structured impact-target mechanic, and this migration adds, removes, and redesigns none — it only moves object-operation authority and corrects authoritative Ground placement.

## MIGRATION METHOD

First make THROW inert. Then restore capability one authority layer at a time. Every live-mutation checkpoint begins from a previously proven no-mutation state so the cause of the first successful movement is identifiable. Any departure from the DROP sequence below is explicitly justified in the stage's notes; there are two: (a) the diagnostics slice (issue #37) is scheduled early because the unguarded P3 comparison already misreads DROP turns today and would misread THROW validation evidence; (b) Stages 10 and 11 must deploy as one unit because the DROP incident history (2026-07-14 CB replay handoff) proves that partial-split live execution without extraction-layer containment invites surviving-source replay.

## STAGE SEQUENCE

### Stage 1 — Freeze the authoritative THROW contract and scope

- **Objective:** fix the mechanical contract (source, quantity classes, Ground destination, fail-closed set, single-action boundary, no impact-target) and the reuse-not-reinvent architecture decision.
- **Authority transition:** none (documentation of intent).
- **Dependencies:** research-notes(226) with closure addenda; DROP precedent documents; current-source verification.
- **Validation gate:** user review of the implementation plan's frozen contract and D2 behavior-change table (three intentional divergences from legacy AP THROW are surfaced there for sign-off).
- **Exit condition:** implementation plan + this roadmap APPROVED by the user.
- **Stop/rollback boundary:** any contract ambiguity the D2 table does not determine → NEEDS USER DECISION before any code.
- **Status:** COMPLETE (2026-07-21). Both the implementation plan and this roadmap were revised once after GPT forensic review (impact-target claim narrowed per user's live spoon-at-house evidence; Stage 4 no-op claim narrowed; V27 compound-inertness row added; V7 entire-stack escalation added), then explicitly approved by the user in conversation. No open contract ambiguities remained.

### Stage 2 — Establish baseline source and runtime evidence

- **Objective:** record pre-migration THROW behavior as the comparison baseline: AP handler behavior (wrong-layer destination, hardcoded quantity 1, whole-stack default), `_apExecutedTransfers` stamping, audit `requested_quantity` misreporting, and one observed whole + one observed partial THROW turn's witness output.
- **Authority transition:** none (read-only).
- **Dependencies:** Stage 1 approval; a runnable deployment (runtime rows cannot be closed from source alone — work-environment constraint noted in the plan).
- **Validation gate:** baseline evidence archived (witness/Mother Brain captures) sufficient for later parity comparison; source anchors re-verified.
- **Exit condition:** baseline recorded; no source drift from the plan's anchors.
- **Stop/rollback boundary:** material anchor drift → NEEDS SOURCE RE-VERIFICATION of the plan before proceeding.
- **Status:** COMPLETE (2026-07-21). Captured live against a locally-run isolated server instance (port 3001, `throw-migration-branch`, pre-quarantine code) via direct adaptive turn-by-turn play — not a scripted harness file. Three classes recorded with real object IDs and witness data:
  - **Partial-quantity defect** — session `1784659562098-rsssplmj0`: `obj_c757a03911bf` "rock" qty 5, `player/player`. Command "I throw 3 rocks onto the ground." Result: source landed at qty **4** (not 2) — AP hardcoded the split-extract quantity to 1 regardless of the requested 3 (`ActionProcessor.js:695`). Successor `obj_814f9dd686b4` qty 1 at `grid/LOC:7,5:19,8`, correct `parent_object_id` lineage.
  - **Whole-object control** — same session: `obj_943d5bc68cc6` "spoon" qty 1, `player/player`. Command "I throw the spoon onto the ground." Result: same object ID moved intact to `grid/LOC:7,5:19,8` — correct Ground at L0 (open ground, no active site/localspace). Witness confirmed `ap_executed_transfer_ids: ["obj_943d5bc68cc6"]`, count 1.
  - **Wrong-layer defect** — session `1784661280145-ufmvr1i9x`, L1 (`active_site: M2x7:site_start`, "Spooner's Diner"; player container `site/M2x7:site_start:4,8`): `obj_27862089d196` "spoon" qty 1. Command "I throw the spoon at the counter." Result: same object ID landed in `grid/LOC:2,7:89,55` instead of the correct site container — confirms the exact destination-layer defect `resolveCurrentGround` (localspace → site → grid) is designed to fix. Identity preserved; no CB duplicate on this particular run.
  - **Side observations, out of scope, not acted on:** narration disagreed with authoritative state on the partial-stack turn (claimed "one rock left" vs actual qty 4 — reinforces why validation uses ORS, not prose); Authority Gate returned `freeform`/`unsupported_referenced_object` for the wrong-layer turn yet AP executed the transfer anyway (witness-flagged `gate_denied_but_executed`) — pre-existing AG/AP behavior, unrelated to THROW ownership, explicitly not touched by this migration (Scope Exclusion 6).
  - Full session IDs, exact commands, before/after state, and raw witness JSON are additionally preserved in the implementing session's task record.

### Slice D1 (independent) — Issue #37 diagnostics guard

- **Objective:** add the operation-family guard to `_buildP3ApTlsComparison` (diagnostics.js) so non-TAKE turns fail closed as `skipped_not_applicable`, mirroring the existing `_buildPartialStackComparison` guard. Diagnostics-only; never part of THROW mutation authority.
- **Authority transition:** none (diagnostics truthfulness only).
- **Dependencies:** none on any THROW stage; may land any time, recommended before Stage 7 so observe-only THROW validation evidence is not contaminated by fabricated P3 mismatches. Already justified independently: the gap misreads DROP turns today.
- **Validation gate:** plan row V22 — non-TAKE families return the stable skipped shape naming the family; TAKE output byte-equivalent to pre-change.
- **Exit condition:** V22 PASS; issue #37 closable.
- **Stop/rollback boundary:** any TAKE output delta → revert the guard; single-file rollback.
- **Status:** COMPLETE (2026-07-22). Landed ahead of Stage 12 per the user's explicit sequencing decision (Stage 12 lists "Slice D1 landed" as a hard dependency, and V22 specifically validates this guard). Confirmed via direct read that `_buildP3ApTlsComparison` (diagnostics.js:1318-1476) had **no** operation-family guard at all — only a `v1 === null` gate and an `!apActuals` gate, then straight into TAKE-shaped field extraction; a DROP/THROW turn producing both `v1` and its own differently-shaped `apActuals` quarantine receipt would fall through and be compared against TAKE-shaped fields, manufacturing a misleading mismatch verdict. Inserted an 18-line pure-addition guard immediately after the two evidence-presence gates (diagnostics.js:1339-1356): `if (v1?.operation_family !== 'take')` returns `skipped_not_applicable` naming the actual family, mirroring `_buildPartialStackComparison`'s existing guard in substance (not literally — that guard lives in a different function with a `mode`/`effectiveMode` parameter P3 doesn't have; the new guard uses P3's own simpler established return shape). Live-verified (11/11 checks): a partial THROW turn → `skipped_not_applicable`/`operation_family_throw_not_supported`; a partial DROP turn → same shape naming `drop`; a partial TAKE turn → guard does **not** fire, reaches the real TAKE-shaped comparison (`outcome_mismatch`, an unrelated pre-existing AP/TLS dynamic, not caused by this guard); a no-`tls_instruction_v1` turn → the original `no_tls_instruction_v1` branch fires unshadowed. TAKE byte-equivalence confirmed via diff, not just live testing: the guard is a pure insertion (zero `-` lines), so every existing TAKE-path line is untouched and a `take` turn falls through into byte-identical pre-D1 logic. `node --check` clean. Not yet committed at the time of this entry.

### Stage 3 — Quarantine ActionProcessor THROW ownership

- **Objective:** insert the four-field refusal stub (`operation_family:'throw'`, `routing:'quarantined'`, `helper_method:null`, `outcome:'refused_ownership'`) at the top of AP's THROW branch; legacy mechanics become unreachable and remain untouched.
- **Authority transition:** AP loses THROW execution; no layer gains it yet — THROW becomes a recognized no-op.
- **Dependencies:** Stage 2 baseline.
- **Validation gate:** plan rows V1 (receipt exact), V26 (syntax/diff scope).
- **Exit condition:** stub live; AP performs zero THROW mutation.
- **Stop/rollback boundary:** revert = remove the stub (legacy code is intact beneath it, restoring prior behavior exactly). Stop if the AP branch shape has drifted from the plan's anchor.
- **Status:** COMPLETE (2026-07-21). Stub inserted at `ActionProcessor.js:667` (7 lines; legacy 668-758 left byte-untouched and unreachable). `node --check` passed. Diff scope confirmed exactly `ActionProcessor.js`. Live-verified on the isolated test server: `state._apActuals` = `{operation_family:'throw', routing:'quarantined', helper_method:null, outcome:'refused_ownership'}`, `ap_executed_transfer_count: 0`. One transitional-exposure observation (expected, not a defect, documented under Stage 4 below): the thrown object still moved via post-narration Continuity Brain extraction on both initial test turns, since no bridge seal exists yet. Source change written and passing; not yet committed (commit is a deliberate separate step per user instruction).

### Stage 4 — Prove AP quarantine (engine-lane invariance)

- **Objective:** demonstrate that ActionProcessor no longer mutates: exact engine-lane ORS invariance for THROW attempts across whole, partial, and unsupported input classes. This stage does **not** claim a full-turn controlled no-op — full-turn inertness (narration seal + CB containment) is established at Stage 6 and proven at Stage 7 (V11).
- **Authority transition:** none (proof stage).
- **Dependencies:** Stage 3.
- **Validation gate:** plan row V2 — identical membership, quantities, object count; empty THROW audit — scoped explicitly to the engine lane. **Known transitional exposure (mirrors DROP's equivalent phase):** no narration seal exists yet, so narration may still describe success and CB may act on that narration. This exposure is dev-only and is the reason Stages 3-6 should be traversed promptly.
- **Exit condition:** V2 PASS on the engine lane; any observed CB-lane mutation documented and carried as Stage-6 motivation, not treated as a Stage-4 failure of the stub — and not represented as a passed no-op either.
- **Stop/rollback boundary:** any engine-lane mutation → stop immediately; the quarantine is defective.
- **Status:** COMPLETE (2026-07-21). Judged by attribution, not final ORS state, per explicit methodology correction (fresh isolated session per input class, since Continuity Brain can still mutate objects downstream of AP during this transitional window and would contaminate an end-state-only judgment). Three input classes tested, each in its own fresh session:
  - Whole-object — "leather satchel" (single object), command "I hurl the leather satchel as far as I can." — `parsed_action: throw`; `_apActuals` exact four-field receipt; `ap_executed_transfer_count: 0`; no AP-authored audit entries.
  - Partial-stack — "5 coins" stack, command "I throw 3 coins onto the ground." — same receipt shape; count 0; no audit entries.
  - Unsupported/vague quantity — "5 coins" stack, command "I throw some of the coins away." (`quantity_mode: unspecified`) — same receipt shape; count 0; no audit entries.
  All three: zero AP-originated ObjectHelper calls, zero `_objectRealityDebug.audit` entries attributable to AP (confirms the unreachable legacy code truly never executes regardless of input shape). One test-methodology note, not a code finding: an earlier attempt using the phrase "I throw the hat onto the ground" was parsed as `operation_family: 'drop'` rather than `'throw'` — pre-existing LLM parser ambiguity between THROW and DROP example phrasing ("onto the ground" overlaps DROP's own instruction text), out of scope, not investigated further.

### Stage 5 — Preserve the narrow AP recognition/refusal receipt

- **Objective:** confirm the four-field receipt is the permanent AP THROW surface: recognition + refusal evidence only, distinguishing intentional quarantine from parser/routing failure.
- **Authority transition:** none (the stub from Stage 3 is the mechanism; this stage locks its contract).
- **Dependencies:** Stage 4.
- **Validation gate:** V1 across supported and unsupported THROW inputs; receipt visible in witness surfaces.
- **Exit condition:** receipt contract documented as permanent (mirrors DROP roadmap step 10's end-state: AP retains recognition and evidence, never semantic or mutation ownership).
- **Stop/rollback boundary:** if any downstream logic is found gating on the receipt as an execution precondition → stop; authority-model violation.
- **Status:** COMPLETE (2026-07-21). Closed on the same evidence set as Stage 4, by user agreement: the receipt was byte-identical in shape across three genuinely different input classes (whole-object, explicit partial quantity, vague/unsupported quantity) — proving it is a deliberate, input-independent quarantine marker rather than a parser-failure artifact. No downstream execution-gating exists to violate: source-level verification during planning already confirmed `ObjectOperationResolver.js`, `TlsObjectOperationExecutor.js`, and `ObjectOperationBridge.js` have zero THROW footprint as of this stage, so nothing in the current codebase could consume `_apActuals` as an execution precondition yet. This closes Slice T1 (Stages 3-5) as one validated unit.

### Stage 6 — Connect single-action THROW to TLS in observe-only mode

- **Objective:** land the full decision/evidence path with zero mutation: `_throwPolicy` + `resolvePlayerHeldThrow` (resolver), THROW family branch with `_deriveThrowEffectiveQuantity` (validation), executor allowlist + family-point extensions (P4 predicts, never executes), index.js resolver branch, bridge THROW branch with `throw_dry_run_seal`, seal derivation + CB mutation-array zeroing extension, alignment THROW labeling. P2 assembly needs no change (verified family-agnostic).
- **Authority transition:** TLS gains THROW resolution/validation/prediction authority; execution authority still nowhere — dry-run only, bridge seal active on every THROW turn.
- **Dependencies:** Stages 3-5; Slice D1 recommended landed.
- **Validation gate:** syntax checks; source review of the five executor family points (re-grep required); V12 (seal scope), V26.
- **Exit condition:** THROW turns produce resolver evidence, v1 instruction, dry-run prediction, seal receipt, truthful no-execution narration constraint — and zero mutation.
- **Stop/rollback boundary:** rollback = remove the resolver branch (index.js) — with no live blocks, all other additions are inert. Stop if executor re-grep finds unverified family points or the bridge branch cannot mirror DROP's structure.
- **Status:** IMPLEMENTATION COMPLETE, NARRATION PARTIALLY TRUTHFUL (2026-07-21). Not yet committed. Stage 7 has not started.
  - **Implementation:** `_throwPolicy`/`resolvePlayerHeldThrow`/`_deriveThrowEffectiveQuantity`/family branch (`ObjectOperationResolver.js`); five family-point extensions, re-grep confirmed exhaustive (`TlsObjectOperationExecutor.js`); resolver branch, seal derivation, alignment branch (`index.js`); THROW branch mirroring DROP's exactly, `throw_dry_run_seal` field (`ObjectOperationBridge.js`). All `node --check` clean.
  - **Two real gaps found beyond this plan's cited anchors, both resolved as narrow completions of already-approved decisions, not new architecture:** (1) `ObjectOperationResolver.js`'s `orsQuantity`/`invalid_source_quantity` strict-stack-quantity gate was literal-gated to `operationFamily === 'drop'` only; extended to include `'throw'` — D2 already decided THROW inherits DROP's strict quantity contract, this gate was simply missed in the plan's prose. (2) The `_dropDryRunSealActive` full-turn seal mechanism has **17 real consumer sites** in `index.js`, not the 2 the plan cited — including one genuine `ObjectHelper.transferObjectDirect` mutation call (emote-removal path), CB candidate/transfer/condition-update/retirement suppression, and two TSL fission/extraction injection guards. All 17 read and confirmed generic (no DROP-specific logic beyond naming) before extending to `_throwDryRunSealActive`; one naming-sensitive diagnostic archive object (`drop_dry_run_seal`) got a parallel `throw_dry_run_seal` object rather than being overloaded.
  - **Live-verified, whole-object observe-only, session `1784665022761-2n96ai5am`:** threw a single held object; resolver evidence, `tls_instruction_v1`, dry-run, and bridge all produced correct THROW-family output; **the object stayed `player/player` despite the narrator describing it landing** — the deterministic seal held on the first live attempt. DROP and TAKE regressions (partial-split 3→2 with correct lineage; ordinary take) confirmed unaffected by the shared-code extensions.
  - **Narration truthfulness — investigated in depth, partially resolved, one residual limitation knowingly accepted and deferred:**
    - Root cause proven directly from the complete rendered narrator prompt (retrieved via the pre-existing `/diagnostics/context?level=narrator_io` endpoint — no new instrumentation needed): `_doIntentBlock` declared `VALIDATED ACTION: throw` "the only mechanical reality" and invited the model to use the player's raw phrasing "freely," positioned well before the bridge's actual no-execution instruction arrives near the prompt's tail — a genuine internal contradiction, not mere instruction-weakness.
    - Also proven: `_freeformBlock`'s bridge-active denial branch (TAKE-flavored "gathering, picking up, taking, holding, receiving, possessing" language) is a **general, family-blind catch-all**, not TAKE-gated — confirmed by reading the complete ternary chain. Further proven that `player_intent.kind === 'FREEFORM'` is the **universal default** for every action except `move`/`enter`/`exit` (`getActionKind()`, unconditional) — so this branch fires identically for DROP and TAKE denials too, not a THROW-specific misroute.
    - **Fix retained:** `_doIntentBlock` now states, only when the bridge is active, that VALIDATED ACTION identifies the attempt and does not by itself mean it executed, deferring to the Object Operation Result; `_freeformBlock`'s bridge-active branch now states the family-neutral principle ("the attempted object operation did not execute... do not describe success, partial success, or any resulting state change") instead of the TAKE verb list. Both are additive and bridge-conditional; the bridge-inactive branches are verified byte-identical to pre-fix text (confirmed directly from a captured live prompt on a normal successful TAKE turn).
    - **Result of the retained fix, six-class live test:** plain denied THROW, denied DROP, denied TAKE, successful DROP, and successful TAKE all narrate truthfully (5 of 6). Vivid denied THROW ("hurl it as far as I can") still narrates false success.
    - **Second, more aggressive fix attempted and reverted:** additionally constraining the word "freely" itself during bridge-active turns, restricting player-phrasing color to the attempt only (not release/flight/impact/landing). Retested the vivid THROW scenario three times on fresh sessions: **3 of 3 still failed** — the knife still narrated leaving the player's hand and landing, only with weaker/clumsier framing. No compliance benefit, demonstrated prose-flattening cost. Reverted; `_doIntentBlock` restored to the first (retained) fix's text.
    - **Accepted, explicitly deferred limitation:** vivid, physically-loaded THROW phrasing can still produce false-success narration even though ORS remains correct and the deterministic seal prevents any actual mutation. This is treated as a known architectural limit of routing mechanical object operations through the same broad FREEFORM narrator framing used for genuinely improvisational input — not a Stage 6 defect to keep patching. The user has scoped a **future, separate project** (after this migration completes) to make TAKE/DROP/THROW genuinely mechanical and narrate them from authoritative state and execution receipts only, excluding them from FREEFORM treatment entirely. This is not part of the current migration and does not block it.

### Stage 7 — Validate dry-run evidence, diagnostics, narration constraints, and zero mutation

- **Objective:** prove TLS understands THROW: correct source ID, authoritative quantity, effective quantity per the frozen table, whole/partial/over routing, Ground destination per layer, fail-closed classes, ambiguity override, and full-turn zero mutation under the seal.
- **Authority transition:** none (proof stage).
- **Dependencies:** Stage 6.
- **Validation gate:** plan rows V3-V12, V20, V21, V27 (compound-turn full-turn inertness — if the thrown object mutates during a compound turn, stop for a bounded containment decision per Stop Condition 10b); TAKE/DROP regressions V23, V24. This is the stage where parser "all"/`selection_mode` runtime behavior for THROW (plan Uncertainties 1-2) is first observed. Individual fail-closed outcomes are acceptable passes, **except**: if normal entire-stack phrasing consistently fails closed because the parser never emits the metadata, that is NEEDS USER DECISION (plan Stop Condition 10a), not a completed entire-stack contract.
- **Exit condition:** all listed rows PASS, including full-turn zero mutation (V11) — the seal now covers the Stage-4 exposure.
- **Stop/rollback boundary:** any mutation on an observe-only turn → stop immediately (V11 fail). Any TAKE/DROP regression → stop.
- **Status:** AUTHORITY VALIDATION PASSED WITH EXPLICITLY ACCEPTED EXCEPTIONS (2026-07-22). Not yet committed at the time of this entry. Not an unqualified full pass — three rows required a user decision rather than a clean PASS; all three were explicitly resolved by the user, not silently accepted by Coding.
  - **D1 status check (required before this entry could be written):** Slice D1 (issue #37 guard) has **not landed** — `_buildP3ApTlsComparison` in `diagnostics.js` still has no `operation_family` guard as of this commit. Issue #37 remains fully open. Nothing in Stage 7's evidence used or touched that function (evidence came from `/diagnostics/context?level=narrator_io`, `/debug/witness`, and `tls_ors_alignment`); Stage 7's clean diagnostics results must not be read as implying issue #37 is fixed.
  - **Clean passes, live-verified with real evidence:**
    - V3a-c: article, explicit-partial, explicit-exact-stack quantities all reached THROW's resolver correctly once tested with clean verb-first phrasing (`throw_default_one`/`parser_explicit` bases, correct effective quantities and `requested_vs_available` classification).
    - V5: vague "some" quantity fails closed (`unsupported_quantity_mode`), no mutation.
    - V8: over-stack THROW denial proven end-to-end — resolver captures the explicit over-request, dry-run returns `operation_allowed: false`/`outcome: fail_closed`/`fail_closed_reason: over_stack`, zero execution.
    - V11: observe-only THROW is mutation-free in the single-action lane — source object confirmed unchanged in ORS, `throw_dry_run_seal: true`, `ap_executed_transfer_count: 0`.
    - V12, V20: compound queues containing THROW correctly never invoke the THROW resolver (`resolver_evidence`/`tls_instruction_v1` both absent) — the single-action boundary held.
    - V21: `tls_ors_alignment` and witness family surfaces label observe-only THROW turns truthfully (`not_executed`/`throw_tls_dry_run_no_transfer_expected`), not the generic non-object mislabel.
    - V23, V24: TAKE and DROP controls (partial, whole/exact-stack, over-stack) all behaved identically to pre-migration expectations. **No new TAKE or DROP regression attributable to the THROW migration was observed in the Stage 7 cases.** (Not "no regression anywhere" — issue #38, the pre-existing TAKE double-split bug found during manual play on 2026-07-21, still exists; it simply did not reproduce in these specific V23 cases and is independently tracked, unrelated to anything Stage 6/7 touched.)
  - **Explicitly accepted exceptions, each resolved by direct user decision, not a silent pass:**
    1. **V3d/V24 "all" sub-case — entire-stack natural-language routing remains blocked.** `quantity_mode:'all'` is correctly captured by the parser with clean verb-first phrasing, but the resolver's "all" branch requires `selection_mode === 'all_from_stack'`, which no parser instruction for TAKE/DROP/THROW ever actually emits — confirmed as a genuinely cross-family gap (reproduced for DROP too, session `1784693286090-02a2m0dai`: "drop all pebbles" correctly failed closed with zero mutation, matching THROW's identical failure). Tracked as issue #40. Pre-existing, not introduced by this migration — the original research flagged this exact gap as "dormant, unexercised" before planning began.
    2. **V6 — zero/invalid quantity was source-validated rather than live-proven.** Natural-language attempts to construct a malformed-quantity THROW ("throw 0 coins") caused the parser to fail action classification entirely rather than exercise the intended `invalid_quantity` code path. The code itself (`_deriveThrowEffectiveQuantity`'s exact-mode branch) is the same logic already validated for DROP; this is a testability limitation, matching the same class of hard-to-construct-fixture exception already accepted for V9/V10 in DROP's own precedent.
    3. **V27 — compound-queue full inertness fails, and this is a state-mutation limitation, not merely another narration gap.** For a compound queue containing THROW ("take a pebble and throw a coin"), the original held object stack stayed completely unchanged (confirmed in ORS) and TLS/AP/bridge were all correctly inactive (`resolver_evidence: false`, `tls_instruction_v1: false`, no seal) — the single-action boundary held exactly as designed. But because the bridge seal only activates for single-action turns, nothing suppressed Continuity Brain's own narrative-driven promotion, and CB **independently created a real, new, authoritative ORS object** (`source: "continuity_brain"`, a duplicate "coin" on the grid, session `1784692895407-6uictb0bl`) based on its own reading of the narration describing the throw. This is not narration lying about a fake outcome — it is a genuine second object entering durable state through an unprotected door. Explicitly accepted and deferred by the user's direct decision, not resolved by Coding. The future mechanical-verbs/anti-FREEFORM project (see O5) must include CB containment for this case specifically, not just narrator-prompt cleanup, when that project is scoped.
  - **V9, V10:** validated via source review only (deterministic-ambiguity and invalid-destination logic are unchanged, reused DROP mechanisms); no live fixture attempted, matching the same accepted precedent DROP's own validation used for equivalent hard-to-construct edge cases.
  - Stages 8-13 remain unstarted.

### Stage 8 — Enable whole-object THROW through TLS → ObjectHelper

- **Objective:** activate the simplest live case: one whole object or exact/all whole stack, player → current Ground, via `transferObjectDirect` exactly once, reason `tls_whole_object_throw`, consuming the P4 prediction; `_tlsExecutionResult` written; Authority Gate compensation reused.
- **Authority transition:** first live mutation authority: TLS → ObjectHelper for whole-object THROW only. Partial remains observe-only. Bridge seal deactivates only for an actually executed, corroborated whole THROW; all denied/unresolved cases keep the seal.
- **Dependencies:** Stage 7 complete from a proven no-mutation state.
- **Validation gate:** V13, V14, V21, V23, V24, V26.
- **Exit condition:** whole THROW live and validated; every non-executed THROW class still sealed and non-mutating.
- **Stop/rollback boundary:** rollback = remove the whole-THROW live block (system returns to Stage-6 observe-only). Stop on: object recreation instead of ID movement, wrong-layer placement, more than one helper call, AG inspecting uncompensated post-transfer inventory.
- **Status:** COMPLETE (2026-07-22). `index.js` whole-THROW live block implemented as a sibling branch to DROP's whole-transfer block (mirrors its structure exactly: consumes the P4 prediction verbatim, calls `ObjectHelper.transferObjectDirect` exactly once, writes `_tlsExecutionResult`, sets `_authorityGateWholeDropObjectId` for AG compensation, reason `tls_whole_object_throw`). Syntax-checked (`node --check`); diff confirmed additive-only to `index.js`. Live-verified against exact-count and qty-1 whole-stack cases (see Stage 9 evidence below). Not yet committed at the time of this entry.

### Stage 9 — Validate whole-object THROW

- **Objective:** prove identity (original ID moves), current-Ground containment per layer (localspace / site / L0), Authority Gate behavior, narration matching authoritative outcome (generic post-mutation INVENTORY/GROUND grounding — no new work, provided layer-correct placement), persistence across save/reload, and TAKE/DROP/no-duplication regressions (CB re-narration of a moved object must be absorbed by the dedup guard / fail ObjectHelper source validation — `promoted === 0`).
- **Authority transition:** none (proof stage).
- **Dependencies:** Stage 8.
- **Validation gate:** V13, V14, V17 (whole-turn half), V21, V23, V24, V25.
- **Exit condition:** all rows PASS at all three Ground layers.
- **Stop/rollback boundary:** any duplication, wrong-layer, or persistence failure → stop; whole-THROW live block reverts to observe-only while diagnosed.
- **Status:** SPLIT VERDICT (2026-07-22) — not an unqualified pass. The written exit condition and V17 were tripped; this is recorded honestly rather than folded into a passed-with-footnote entry like Stage 7's.
  - **TLS → ObjectHelper → ORS mutation authority: PASS.** The exact-count whole-stack case was the one row not yet covered by earlier qty-1 testing, and it was run specifically because GPT flagged the gap before Stage 9 could be called closed. Founded a 5-unit `coins` stack (`obj_31bcece41261`); command `throw 5 coins`; `tls_executor_dry_run` predicted `whole_transfer`/`transferObjectDirect`; `tls_execution_result.transfer.result: "success"`; the original object moved intact — same ID, quantity still 5 — to the correct Ground layer via `tls_whole_object_throw`, exactly one helper call, no successor created. Qty-1 whole-object cases (spoon, brass key) at all three Ground layers (localspace/site/L0) also passed identically. Persistence confirmed across save/reload. Authority Gate compensation confirmed (V14).
  - **V17 whole-turn zero-duplication requirement: FAIL.** The same exact-count test that proved the authority chain correct also surfaced three new objects — `obj_0b540018e8fb` (qty 3), `obj_c1c6fca60fb3` (qty 1), `obj_5c34b20ee7a7` (qty 1), all `source: "continuity_brain"`, all created on the throw turn, all `action: "promoted"` directly into the same grid container the real stack landed in. 3+1+1 = 5 — CB independently promoted objects representing the same five coins the narrator described landing individually, on top of the stack TLS had already moved correctly. Full mechanism, evidence, and root-cause tracing filed as issue #42 (opened this entry).
  - **Why this is not the same finding as V27/O7:** V27 is a compound-queue turn, where the seal never activates by design — an acknowledged, out-of-scope gap. This was a plain single-action `"throw 5 coins"` turn, where the seal applied and worked correctly right up until the transfer executed, then correctly deactivated (a real transfer had happened). The duplication came from CB's ordinary post-execution narrative extraction acting on the same turn's own narration, and it slipped past the generic dedup guard on a plural/singular name mismatch (`"coins"` vs `"coin"`) rather than an unguarded window. A future fix that only suppresses CB during the pre-execution/observe window would not catch this — it would additionally need to suppress CB's promotion of the specific object identities/quantities just mechanically handled, for the remainder of that same turn.
  - **Why the branch continues past this failure:** the plan's own Stage 11 scope (written before this session began implementing) already named Bug A — new duplicate promotion via CB, as opposed to Bug B's surviving-source replay — and excluded it by name, tracking it as follow-up O2. This finding is a live confirmation of that already-anticipated, already-out-of-scope gap, sharpened with a precise mechanism, not a new gap introduced by this migration. The TLS/ObjectHelper/ORS chain that this migration is actually building did not cause it and is not implicated by it — none of its diagnostic counters or alignment fields are touched by CB's independent promote path. Continuing to Stage 10/11 on their original scope, without expanding Stage 11 into general CB containment, is an explicit user decision, not a silent pass. See issue #42 and plan O8 for the full record this decision rests on.
  - Stages 10-13 remain unstarted.

### Stage 10 — Enable partial-stack THROW through the existing split path

- **Objective:** activate `splitObjectDirect` for approved partial predictions, reason `tls_partial_stack_throw`: source persists player-held at the reduced quantity; exactly one successor with lineage at the predicted Ground; shared `tls_partial_stack_execution_v1` envelope + request-local archive lifecycle (already family-neutral); description target capture; quantity-one successor rename; successor description clear; THROW CB receipt capture. Do not restore AP's one-unit special case.
- **Authority transition:** TLS → ObjectHelper split authority for THROW.
- **Dependencies:** Stage 9 complete. **Deployment constraint:** Stage 10 must not reach a deployed/playable build without Stage 11 in the same unit (justified departure from strict step ordering — the DROP rocks incident is direct evidence of Bug-B replay when a partial split ships without extraction-layer containment).
- **Validation gate:** V15, V26 (structural); full behavioral gate deferred to Stage 12 by design.
- **Exit condition:** partial split executes once with correct arithmetic, lineage, and containment in dev validation.
- **Stop/rollback boundary:** rollback = remove the partial live block (whole THROW unaffected). Stop if the split cannot consume predicted parameters verbatim.
- **Status:** COMPLETE (2026-07-22, one deployment unit with Stage 11). `index.js` partial-THROW live block added as a sibling `else if` to the DROP partial block — exact mirror: consumes the P4 dry-run prediction, calls `splitObjectDirect` once with reason `tls_partial_stack_throw`, writes the shared `tls_partial_stack_execution_v1` envelope, captures the description target, applies the quantity-one successor rename via the reused family-agnostic `_singularizeDropSuccessorName`, and captures the THROW CB receipt. Live-verified (V15) across 6 partial throws in fresh isolated sessions (port 3001): coins 5→3/succ 2, coins 6→3/succ 3, stones 5→3/succ 2, marbles 7→3/succ 4, nails 5→3/succ 2, plus a partial DROP control 5→3/succ 2. Every case: source persists player-held at before−n, exactly one `partial_split` successor at the correct grid Ground with `parent_object_id` = source and the requested quantity, total quantity conserved. AP's one-unit special case was not restored. Syntax-checked; diff additive, scoped to `index.js` + `ContinuityBrain.js` only.

### Stage 11 — Integrate description-reconciliation and Continuity Brain protections for partial THROW

- **Objective:** land the full Bug-B containment chain and description handling: request-local THROW receipt slots + capture + sanitizer (index.js); `tlsPartialStackThrowReceipt` into `runPhaseB`; ContinuityBrain second sanitizer, prompt receipt block + precedence rules at the DROP block's tier, `partial_throw_successor_description` extraction; index.js post-CB successor-description consumption, third source-normalization call, and the deterministic replay-suppression filter keyed to the THROW receipt's source ID + exact containers (transfers only, successor never matched, promote path untouched).
- **Authority transition:** none gained; CB's ability to replay the surviving source as a transfer is deterministically removed for receipt-matched entries. **Two failure modes remain distinct:** Bug B (surviving-source transfer replay) is closed by this chain; Bug A (new duplicate promote) is mitigated by layer-correct placement + the existing generic dedup guard, with the residual identity-precision gap explicitly out of scope (shared pre-existing limitation, tracked as follow-up O2 in the plan).
- **Dependencies:** Stage 10 (same deployment unit).
- **Validation gate:** V16, V17, V18, V19, V26.
- **Exit condition:** replay suppression proven against a live CB replay attempt (or a constructed quarantine fixture if CB does not emit one during validation); descriptions reconcile; unrelated CB operations untouched.
- **Stop/rollback boundary:** stop if the prompt block cannot be inserted without restructuring the extraction prompt, or the suppression predicate cannot reject foreign/contradictory receipts from existing fields. Rollback of Stage 11 requires rollback of Stage 10 (unit rule).
- **Status:** COMPLETE (2026-07-22, one deployment unit with Stage 10). Full Bug-B containment chain cloned end-to-end from DROP with THROW identity, per plan D7: request-locals + `_sanitizeCbTlsPartialStackThrowReceipt` + `_captureCbTlsPartialStackThrowReceipt` (single-capture state machine, schema `cb_tls_partial_stack_throw_v1`, operation `tls_partial_stack_throw`) in `index.js`; `runPhaseB` `tlsPartialStackThrowReceipt` option; `ContinuityBrain.js` second sanitizer, authoritative PARTIAL THROW RECEIPT context block, PARTIAL THROW RECEIPT PRECEDENCE + PARTIAL THROW SUCCESSOR DESCRIPTION rule text (pattern-based, no literal gameplay examples), `partial_throw_successor_description` extraction field and return; `index.js` post-CB successor-description consumption, a third `_normalizePartialSplitSourceDescription` call, and the deterministic replay-suppression filter keyed to the receipt's `source_object_id` + exact source/destination containers (transfers only, successor never matched, promote path untouched).
  - **V18 descriptions — PASS (live):** partial THROW of a described stack normalized the surviving source ("five smooth stones…" → "smooth stones…", leading count stripped) and gave the successor a distinct, narration-grounded child description via the `partial_throw_successor_description` channel rather than inheriting the parent's stale text.
  - **Seal corroboration gate — PASS (live):** a denied over-stack partial THROW kept the bridge `active:true` / `throw_dry_run_seal:true` with zero mutation; a corroborated partial success lifted the seal. The seal never lifts on prediction alone — the bridge's `partial_throw_execution_confirmed` predicate is the full DROP-identical corroboration (schema, `executed===true`, `split_result.ok===true`, reason match, prediction-scalar equality, arithmetic identity, distinct IDs, `source_quantity_after > 0`).
  - **V23/V24 regressions — PASS (live):** partial TAKE (uniform 5-stack → take 2) and partial DROP unchanged.
  - **V16 Bug-B replay suppression — PASS (bounded white-box fixture).** CB emitted no natural source-replay across the 6 partial-throw validation runs (`suppressed_replays: none` every time), so per this stage's exit condition a constructed quarantine fixture was used. The fixture drives the **verbatim** sanitizer + filter code from `index.js` against a **real** post-split ORS state captured from a live partial-throw autosave, and proves both directions: the true source-replay (player→Ground, exact receipt match) is deterministically suppressed and logged with reason `cb_partial_throw_source_replay_suppressed`; every non-matching entry is preserved — the successor itself (never matched), an unrelated object, the source with a wrong destination, a `temp_ref` entry, a **promote** of the source (promote path untouched), and the source from a non-player container. The sanitizer additionally rejected 7 tampered receipts against live ORS and did not run at all when the receipt state was not `accepted`.
  - **Bug A (issue #42) explicitly not addressed here.** As the plan's original Stage 11 scope states, this chain closes Bug B only; the new-duplicate-promote path is untouched. Notably, zero Bug-A duplication appeared in any of the 6 live partial throws (unlike the whole-throw exact-count case), most plausibly because the PARTIAL THROW RECEIPT PRECEDENCE prompt tells CB to emit no promotes/transfers/etc. for the receipt-governed operation — but this is an observation, not a containment claim; Bug A remains out of scope and probabilistic.
  - Stages 12-13 remain unstarted.

### Stage 12 — Complete full validation

- **Objective:** run the entire verification matrix: quantity classes (default-one, explicit partial, exact stack, all, over-stack, vague, zero/negative/fractional/malformed), ambiguity, unresolved targets, invalid destinations, all three Ground layers, identity and lineage, surviving-source replay, the two-assertion duplication check (`object_reality.promoted === 0` AND exactly one TLS successor — never collapsed into one assertion), persistence, narration, diagnostics labeling, bridge behavior, issue-#37 guard behavior on THROW turns, single-action scope, TAKE regression, DROP regression, unrelated-CB preservation.
- **Authority transition:** none (proof stage).
- **Dependencies:** Stages 8-11 live; Slice D1 landed.
- **Validation gate:** every plan matrix row V1-V27 PASS, each reported individually with its evidence surface. The V7 entire-stack escalation and the V27 compound-inertness proof are explicitly re-checked here on the final build.
- **Exit condition:** full matrix green; parity with the frozen contract demonstrated; results archived for Mother Brain.
- **Stop/rollback boundary:** any failed row stops Stage 13 unconditionally; a failed row is never summarized as partial success.
- **Status:** MATRIX COMPLETE (2026-07-22). Every row V1-V27 addressed with evidence on the final build (Stages 1-11 + Slice D1 all live in the working tree). The TLS → ObjectHelper → ORS authority chain and every structural guarantee it makes — identity, quantity derivation, Ground containment at all three layers, fail-closed classes, seal corroboration, Bug-B suppression, persistence, diagnostics truthfulness for the guard this stage depended on, TAKE/DROP non-regression — **PASS with fresh live evidence**, not merely carried forward from earlier stages. Four rows carry **pre-existing, already-decided exceptions**, none of them new Stage-12 findings and none of them regressions — each was explicitly surfaced and resolved by direct user decision in an earlier stage, and this stage's job was to re-confirm the mechanism still reproduces identically, not to re-litigate the decision:
  - **V7 / issue #40** — "throw all the pebbles" still fails closed via the resolver's own `contradictory_quantity_metadata` (parser never emits `all_from_stack`); zero mutation held. Re-confirmed identical to the Stage 7 finding.
  - **V17 whole-turn half / issue #42** — not re-tested this stage (already exhaustively characterized at Stage 9); the accepted split verdict stands unchanged. V17's partial-turn half continues to pass cleanly (6+ live partial throws, zero duplication observed, consistent with Stage 11).
  - **V21 live-labeling half / issue #41** — `tls_ors_alignment` mislabeling of successful live whole/partial transfers remains open, diagnostics-only, non-authority (confirmed cross-family at Stage 9); not re-tested live this stage since nothing in Stages 10-12 touches that code path.
  - **V27 / O7** — this run's compound-turn re-check (`"take the pebble and throw the coin"`) showed the core inertness invariant holding perfectly (resolver never invoked, original object end-to-end unchanged) **and, this particular run, zero extra CB-promoted duplicates** — informative, not a fix: O7's mechanism (no seal covers compound queues by design) is unchanged and the risk remains accepted/deferred; the absence of a duplicate this run confirms the gap is probabilistic, not deterministic.
  - **V6, V9, V10** remain validated via source review only, per the plan's own evidence-type legend (SRC is a legitimate evidence class) and the same DROP-equivalent precedent accepted at Stage 7 — unchanged code paths, no new fixture attempted.
  - **One notable non-finding worth recording:** a V7/V11 live run momentarily showed `fail_closed_reason: "missing_source"` in the P4 executor dry-run where `contradictory_quantity_metadata` (V7) or a genuine no-stack founding variance (V11, CB founded "two marbles" as two separate singular objects rather than one stack) was expected. Traced to source and confirmed as correct, by-design cascading behavior, not a bug: `_assembleTlsInstructionV1` (index.js:1608-1613) withholds the source from the instruction envelope whenever the resolver's own evidence already carries a `fail_closed_reason`, and the executor's independent `missing_source` check (`TlsObjectOperationExecutor.js:79-81`) is truthfully reporting that withheld state. The authoritative root cause is always the resolver's own evidence, which in both cases matched its expected reason exactly.
  - **Live evidence this stage (21/21 checks pass):** V4 (default-one bare-noun, 4→3 with qty-1 successor), V7 (zero mutation), V11 (zero mutation + seal active on denial), V12 (compound queue never invokes THROW resolver), V13 (whole-object live, final build), V15 (partial split live, final build), V22 (D1 guard, 11/11 checks), V23/V24 (TAKE/DROP partial regressions, final build), V25 (persistence across save/reload for both whole and partial THROW, no leaked top-level `_tlsPartialStackResult`), V27 (compound-turn inertness re-check).
  - `node --check` clean across every implicated file (`index.js`, `ContinuityBrain.js`, `diagnostics.js`, `ActionProcessor.js`, `ObjectOperationResolver.js`, `TlsObjectOperationExecutor.js`, `ObjectOperationBridge.js`); `git diff --name-only main...HEAD` confirms the full-migration touched-file set matches exactly the plan's implicated-file list (plus `diagnostics.js` for the still-uncommitted D1 slice) — no out-of-scope file has ever been touched.
  - Nothing in this stage is committed yet.

### Stage 13 — Remove or permanently dead-end obsolete AP THROW mechanics

- **Objective:** delete the unreachable legacy AP THROW implementation (the code beneath the Stage-3 stub), correct the stale "same path as drop" comment, and confirm the permanent end-state: AP keeps only the recognition/refusal stub and receipt.
- **Authority transition:** none (dead code removal); the authority model is already final.
- **Dependencies:** Stage 12 fully green — removal is forbidden before TLS authority and parity are proven.
- **Validation gate:** V1 and V19 rerun; V23/V24 rerun; syntax check; diff review confirming only the legacy region was removed.
- **Exit condition:** one authority path for THROW; no transitional or duplicate THROW handling remains; migration complete.
- **Stop/rollback boundary:** if removal reveals any live reference into the legacy region → stop and restore; the region was not dead.

## PERMANENT BOUNDARIES (apply to every stage)

- ObjectHelper/ORS remains the sole mutation authority; AP remains diagnostic-only; narration and CB are never authority.
- No stage may enable compound-command THROW, impact-target semantics, parser changes, ObjectHelper changes, Authority Gate logic changes, or a generalized verb framework.
- Every live-mutation activation begins from the previously proven no-mutation state.
- Denied, unresolved, stale, ambiguous, or unsupported THROW turns remain non-mutating and truthfully narrated at every stage from Stage 6 onward; during the Stage 3-5 transitional window only engine-lane invariance is guaranteed (see Stage 4), and compound-turn inertness is proven by V27 rather than assumed.
- Any newly discovered requirement touching a subsystem this roadmap defers must be reported for approval before implementation.
- Nothing in this roadmap is complete until its validation gate has actually been run and observed; planning is not completion.

## REVISION HISTORY

- **2026-07-22 — Revision 8 (Slice D1 landed; Stage 12 full validation matrix complete).** Landed Slice D1 (issue #37 guard) ahead of Stage 12 per the user's explicit sequencing decision, since Stage 12 lists it as a hard dependency and V22 specifically validates it — an 18-line pure-addition operation-family guard in `_buildP3ApTlsComparison` (diagnostics.js), live-verified 11/11 (DROP/THROW turns correctly diverted to `skipped_not_applicable` naming the family; genuine TAKE turns unaffected, confirmed both live and via diff showing zero lines of existing TAKE-path code changed). Ran the full Stage 12 matrix: every row V1-V27 addressed with fresh evidence on the final build. 21/21 live checks pass across V4, V7, V11, V12, V13, V15, V23, V24, V25, V27, plus D1's 11/11 for V22. Investigated and resolved a `missing_source` anomaly surfaced during V7/V11 testing — traced to source (`_assembleTlsInstructionV1` withholding the source when the resolver's own evidence already fail-closed; the executor's independent `missing_source` check truthfully reports that withheld state) and confirmed as correct cascading fail-closed behavior, not a bug; the authoritative resolver-evidence reason matched the expected mechanism in both cases (V7: `contradictory_quantity_metadata`/issue #40; V11: a genuine no-stack founding variance, unrelated to the guard). Four rows carry pre-existing, already-decided exceptions carried forward unchanged (V7/issue #40, V17 whole-turn/issue #42, V21 live-labeling/issue #41, V27-O7); none are new, none are regressions, all were already user-adjudicated in earlier stages. V6/V9/V10 remain source-review-validated per established precedent. Top-level status updated. Stage 13 remains unstarted. No architectural or contract change.
- **2026-07-22 — Revision 7 (Stages 10 + 11 complete as one deployment unit — partial-stack THROW live + Bug-B CB receipt chain).** Partial-THROW live block added mirroring DROP's partial split (reason `tls_partial_stack_throw`); full Bug-B containment chain cloned end-to-end from DROP with THROW identity across `index.js` and `ContinuityBrain.js` (request-locals, sanitizer, capture state machine, `runPhaseB` option, CB receipt context + precedence rules + successor-description field, post-CB consumption, third source-normalization call, deterministic replay-suppression filter). Live-verified: V15 (6 partial throws, correct arithmetic/lineage/containment), V18 (source leading-count normalized, successor child-description reconciled), seal corroboration gate (denied over-stack keeps seal active with zero mutation; corroborated partial lifts it; never lifts on prediction alone), V23/V24 regressions clean. V16 Bug-B replay suppression proven via a bounded white-box fixture (CB emitted no natural replay in validation, so per the Stage 11 exit condition a constructed quarantine fixture drove the verbatim sanitizer + filter against real post-split ORS state, proving both suppression of the true source-replay and non-overreach across successor/unrelated/wrong-container/temp_ref/promote entries, plus sanitizer rejection of 7 tampered receipts). Bug A (issue #42) explicitly untouched, matching the plan's original Stage 11 scope; incidentally not reproduced in any live partial throw (observation only, not a containment claim). Diff additive, scoped to `index.js` + `ContinuityBrain.js`; package.json bumped. Stages 12-13 remain unstarted. No architectural or contract change.
- **2026-07-22 — Revision 6 (Stage 8 complete; Stage 9 closed as an explicit split verdict, not a pass).** Stage 8 marked COMPLETE: whole-THROW live block implemented mirroring DROP's structure exactly, syntax-checked, additive-only diff. Stage 9 marked SPLIT VERDICT per direct user decision, prompted by a GPT question asking whether the exact-count full-stack case (holding 5 coins, "throw 5 coins") had actually been tested before Stage 8/9 could be called closed — it had not; every prior whole-transfer test used a qty-1 object. Running it proved two things at once: the TLS → ObjectHelper → ORS authority chain is correct (original stack ID moved intact via `tls_whole_object_throw`, exactly one helper call), and V17's whole-turn zero-duplication requirement fails — Continuity Brain independently promoted three new objects (summing to the same quantity thrown) from its own reading of the turn's narration, defeating the generic dedup guard on a plural/singular name mismatch (`"coins"` vs `"coin"`). Root-caused as a live confirmation of the plan's already-anticipated, already-out-of-scope Bug-A gap (O2), not a new defect introduced by this migration, and distinct in mechanism from V27/O7 (that gap is an unsealed compound-queue window; this one is same-turn CB re-promotion after a correctly-deactivated seal, following a successful mechanical transfer). Filed as issue #42 with full mechanism and evidence. User explicitly authorized the branch to continue to Stages 10-11 on their original scope rather than treating "any duplication → stop" as ending the branch, and explicitly declined to expand Stage 11 beyond its existing Bug-B-only scope to chase this finding — it is deferred to the future mechanical-verbs/anti-FREEFORM project along with O5/O7. Top-level status updated accordingly. Stages 10-13 remain unstarted. No architectural or contract change.
- **2026-07-22 — Revision 5 (Stage 7 checkpoint: authority validation passed with explicitly accepted exceptions).** Stage 7 run to completion using verb-first phrasing after isolating and separately filing the parser first-token-strip bug (issue #39) that had contaminated earlier quantity testing. Clean passes: V3a-c, V5, V8, V11, V12, V20, V21, V23, V24. Three rows required and received an explicit user decision rather than a silent pass: V3d/V24's "all" sub-case (issue #40, confirmed cross-family by reproducing the identical failure on DROP), V6 (source-validated only, live reproduction impractical), and V27 (a genuine state-mutation limitation — Continuity Brain can independently promote a real duplicate object during compound-queue THROW turns, since the single-action seal does not cover compound queues by design; accepted and deferred to the future mechanical-verbs/anti-FREEFORM project, which must include CB containment, not just narrator cleanup). Confirmed before writing this entry that Slice D1 (issue #37 guard) has not landed, so Stage 7's diagnostics evidence must not be read as implying issue #37 is fixed. TAKE/DROP regression language corrected to "no new regression attributable to this migration was observed" rather than an unqualified "no regression anywhere," since issue #38 (a pre-existing, unrelated TAKE bug) remains open independently. Top-level status and Stage 7 status updated accordingly. Stages 8-13 remain unstarted. No architectural or contract change.
- **2026-07-21 — Revision 4 (Stage 6 implementation + narration investigation reconciled).** Stage 6 marked IMPLEMENTATION COMPLETE, NARRATION PARTIALLY TRUTHFUL (not COMPLETE — Stage 7 has not run). Documents two real plan-vs-source gaps found and resolved during implementation (resolver strict-quantity gate extension; 17-site seal mechanism, not the 2 originally cited). Documents the full narration-truthfulness investigation: proven root cause (`_doIntentBlock` contradiction + family-blind `_freeformBlock` branch + universal FREEFORM classification default), the retained bridge-aware wording fix (5 of 6 test classes now truthful), a second more aggressive fix that was tested three times, produced no compliance benefit, and was reverted, and the one residual limitation (vivid-THROW false-success narration) explicitly accepted and deferred to a future project scoped by the user to remove mechanical object-operation verbs from FREEFORM narrator treatment entirely. Top status line updated to reflect Stage 6's true state and that Stage 7 has not begun. No architectural or contract change to the migration itself.
- **2026-07-21 — Revision 3 (Slice T1 closure; Stages 3-5 complete).** Stage 3 marked COMPLETE: `ActionProcessor.js:667` refusal stub implemented, syntax-checked, and live-verified (receipt exact, zero AP mutation). Stage 4 marked COMPLETE: engine-lane invariance proven by attribution (not final-state) across whole/partial/unsupported input classes in fresh isolated sessions, per explicit methodology correction — final-state judgment would have been contaminated by the documented transitional CB-mutation exposure. Stage 5 marked COMPLETE on the same evidence: receipt shape proven stable and input-independent; no downstream consumer of `_apActuals` exists yet to violate the non-authorization invariant. Overall status remains APPROVED; Slice T1 is fully validated with its source change written and passing but intentionally not yet committed — commit/push is being done as a separate deliberate step per user instruction. No architectural or contract change.
- **2026-07-21 — Revision 2 (status sync; Stage 1-2 closure).** Status changed READY FOR REVIEW → APPROVED per explicit user conversational approval (given as "ceremonial authority" rather than a prior file edit; this revision syncs the text to match). Stage 1 marked COMPLETE. Stage 2 marked COMPLETE with live baseline evidence recorded directly in this document (partial-quantity defect, whole-object control, wrong-layer defect, two out-of-scope side observations). No architectural or contract change — this is a status/evidence update only.
- **2026-07-21 — Revision 1 (post-review; GPT forensic audit + user runtime clarification).** Stage 4 retitled "Prove AP quarantine (engine-lane invariance)" — it no longer claims a controlled full-turn no-op, which is proven at Stage 7; FINAL CONTRACT impact-target sentence replaced with preservation language (existing trailing-target interpretation/narration unchanged; no authoritative structured mechanic exists; migration only moves authority and corrects Ground placement); Stage 7 and Stage 12 gates gained V27 (compound-turn full-turn inertness) and the V7 entire-stack NEEDS USER DECISION escalation; PERMANENT BOUNDARIES non-mutating claim scoped to Stage 6 onward. Status remains READY FOR REVIEW.
- **2026-07-21 — Initial READY FOR REVIEW.**
